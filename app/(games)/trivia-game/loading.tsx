import React from 'react'

/**
 * The function returns a loading component in a React application.
 * @returns A div element with the text "loading" inside.
 */

export default function loading() {
  return (
    <div>loading</div>
  )
}
